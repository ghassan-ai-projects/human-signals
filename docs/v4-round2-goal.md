# Goal, round 2: perfect the three scenes, add an optional Advanced flow

Date: 15 September 2026
Builds on: [v4-prototype-goal.md](v4-prototype-goal.md) (phases 0–8, done) · [v4-prototype-review.md](v4-prototype-review.md)
Lives in: [`prototypes/v4/`](../prototypes/v4/)

## The goal in one line

> **The three existing scenes feel finished: every interaction responds, every transition carries meaning, nothing collides at any supported width. On top, an optional Advanced flow lets a curious or advanced learner go deeper and test themselves, without changing the guided experience for anyone else.**

## Rules for this round

1. **No new cases.** No new triggers, pathways or organs systems. Work only inside stress, meal and dark.
2. **Advanced is optional and off by default.** Nothing in the guided flow is gated behind it, and turning it off returns exactly the guided experience. Its state is remembered locally and erasable.
3. **Advanced stays honest.** Named receptors, molecule classes, transporters and second messengers at undergraduate-physiology level, marked "Advanced · illustrative draft". Still no numbers, doses, gauges, diseases or treatments; time stays in words.
4. **Motion carries meaning, never decoration**, and every animation has a reduced-motion equivalent.
5. **One commit per phase**, verified in a browser with no console errors, with the goal's progress table updated in the same commit.

## Phases

| # | Phase | What ships | Done when |
|---|---|---|---|
| 9 | **Feel** | Labels and hotspots enter softly; hovering a hotspot previews its step and lights the route into it; hovering a route lights it and names the signal, clicking it opens a signal card (the dashed ghost offers Try it?); hovering a trigger or search result previews its organs; the arriving organ brightens once when a pulse lands; the time ribbon glides and can be clicked anywhere; route cross-fades on Fast / Slow; the current step is marked in the pathway bar; a first-load entrance for the body; the pathway bar and ribbon adapt from 1024 to 1920 without touching the zoom controls or mini-map | Scripted check at 1024, 1280, 1440, 1920 shows no bottom-bar collisions in any pathway; reduced motion still passes |
| 10 | **Craft of the illustration** | Refined heart (atria, great vessels), lungs (bronchial tree), brain in the head (hemispheres), kidneys with ureters, silhouette rim light; active routes get a soft static glow; ghost lines breathe once when first shown | Before/after evidence; routes and hotspots stay the most salient things (grayscale and squint check) |
| 11 | **Advanced: foundation and signal passports** | An *Advanced* switch (pathway bar and settings, key A). When on: advanced one-liners at organ and structure level; route labels name the molecule class; a **signal passport** for every signal in the five pathways (what it is made from, where it is made, how it travels, where its receptor sits, how quickly it acts, how it is switched off), opened from the route, the tree or More; a passport comparison table that makes peptide vs steroid vs amine visible | Passports for all 12 signals; guided flow unchanged with Advanced off |
| 12 | **Advanced: Rebuild the route** | A challenge per pathway: routes and numbers hide, the learner taps organs in causal order (with distractors) and names the signal on each link; Check gives specific corrections, then the true route draws on; recorded as practice | Works in all five pathways by mouse and keyboard |
| 13 | **Advanced: Compare routes** | In scenes with two pathways (stress, meal): both routes on one body in distinct styles, a shared ribbon showing where each is active in words, and a side-by-side table (first signal, carried by, reaches, timescale in words, what you'd notice, switched off by) | Fast vs Slow and Between vs After both readable at 1280 |
| 14 | **Cell mechanisms, step by step** | Every cell inset steps through its mechanism (Next / Previous, the active part lit, others dimmed); Advanced adds the named molecules (glucocorticoid receptor, cAMP, GLUT4, AANAT, MT1/MT2…) | All four insets, keyboard and reduced motion |
| 15 | **QA, evidence, publish** | Overlap and bottom-bar audits with Advanced on and off, reduced-motion and keyboard walk-throughs, refreshed evidence, republished artifact, review note updated with the Advanced flow and what it needs reviewed | Artifact link shared; evidence committed |

## Progress

| Phase | Status | Notes |
|---|---|---|
| 9 | Done | Labels and hotspots enter softly; hotspot hover/focus previews the step and lights the route into it; routes have wide hit areas: hover lights them and names the signal, click opens a signal card, the dashed ghost says "Something acts back here · Try it?" and opens it; trigger and search-result hover preview organs; the organ a signal lands on brightens once; route opacity cross-fades; the ribbon glides and accepts clicks anywhere; the current step is filled in the pathway bar; the body settles in on first load. Layout: at ≤1400 px the time chip and "Read the route" text fold away and zoom controls sit above the mini-map; at ≤1180 px the pathway name and Play text fold into icons, toggles use short words and the caption drops below the toolbar. Checked by script: no bottom-bar collisions or overflow in all five pathways at 1024, 1280, 1440 and 1920 |
| 10 | Done | Heart gains its great vessels (aortic arch, pulmonary trunk, superior vena cava) that scale with zoom; lungs a faint bronchial tree; the silhouette a soft inner rim light from the top-left. Active routes carry a quiet static glow that waits until draw-on finishes; a ghost feedback line breathes once when it first appears so the eye finds it. All static or one-shot, none of it encodes amount, and reduced motion skips the breath |
| 11 | Done | *Advanced* switch in the pathway bar and settings (key A), off by default, stored as a preference apart from progress. With it on: advanced one-liners for all 19 hotspots at organ zoom and closer (e.g. "Corticotrophs cut ACTH from POMC and release it"), molecule class on route labels ("cortisol · steroid · blood"), and 12 signal passports (made from, made in, travels, receptor, acts, switched off) with a receptor-location figure (surface, inside, transporter, synapse), a table of the scene's signals and why class matters. Passports open from route cards, tree signal rows, search results and a new Advanced section in More. With it off, every label is exactly the guided text. Label placement now treats the open side sheet, the pathway bar and ribbon, and the zoom controls and mini-map as obstacles, resolves upward after clamping, and lets a route name with nowhere free step aside (hover and Read the route still name it). Checked by script: 0 overlaps across 19 close-up views at 1440 and 1280 in guided and Advanced modes; bottom bar fits at 1024 with the Advanced button |
| 12 | Done | The Advanced button now opens a small menu (mode switch, *Rebuild this route*, *Signal passports*), so the pathway bar keeps its width. Rebuild: routes, numbers and labels hide; candidate organs (the chain plus two that don't belong, in a stable shuffled order) get dashed rings and chips in a docked card; taps place numbered markers on the body (tap again to undo from there). *Check order* gives one specific correction ("After Pituitary comes Adrenal glands, not Kidneys" / "Kidneys isn't part of this route") and marks organs; then the learner names the signal on each link from chips; *Check names* corrects each wrong link ("ACTH, not cortisol, carries the message from Pituitary to Adrenal glands"). Recorded as practice; *Watch the true route* restores and plays the pathway. After a meal accepts liver or muscles as the last stop. Body signs hide during the challenge so none gives an answer away, and return on close. Escape closes. Guard added so a pending autoplay never fires into another pathway or a challenge. Checked by script: correct runs in all five pathways (plus liver as the alternative last stop), a wrong-order and wrong-name run with its corrections, attempts recorded, no console errors |
| 13 | Done | *Compare* in the Advanced menu for stress (Fast and Slow) and meal (Between meals and After a meal): both routes on one body, A solid and B hollow (a wider line with a dark core, so the difference survives grayscale), a legend under the caption, organ labels tagged with the routes that reach them ("Liver · A B"), route names with their letter and molecule class (one per signal per route), no hotspots or signs. A docked card holds a words-only timeline per route (a shared lane grid when the scene has one ribbon, highlighted words when each pathway has its own) and a side-by-side table: first signal, carried by, reaches, timescale, what you'd notice, switched off by, overall. *Play both* plays A's route then B's; Escape or Close restores the pathway. The camera frames the union of both routes beside the card |
| 14 | Done | Each cell inset is now a stepper: the four steps are a numbered list you click, or walk with Next / Previous or the arrow keys; the active step lights and a soft spotlight glides over the part of the cell it is about, dimming the rest. The signal molecule travels its path on step 1 and rests bound afterwards. With Advanced on, each step gains its named molecules (glucocorticoid receptor + GREs for cortisol, Gs/cAMP/PKA + GLUT2 for glucagon, insulin-receptor tyrosine kinase/PI3K/Akt + GLUT4 for insulin, β1/cAMP/AANAT + HIOMT for melatonin). Live-region announces each step. Verified in browser: all four insets have aligned step/focus/adv arrays, click + arrow-key stepping and spotlight glide work, reduced motion snaps instead of animating, no console errors |
| 15 | Done | Scripted audit across all five pathways at 1024, 1280, 1440 and 1920 with Advanced both off and on (40 views): 0 bottom-bar collisions or overflow and no label overlaps; caught and fixed two pre-existing layout bugs (at 1280 the top caption slid under the toolbar; the wide *After a meal* pathway bar touched the zoom control at 1440 — the caption/thought drop and the non-done-chip hide now start at ≤1400/≤1500). Reduced-motion cell stepper walks by keyboard; no console errors. Refreshed evidence added (22–26: Advanced organ labels, a signal passport, Rebuild, Compare at 1280, the cell stepper mid-step with named molecules). Review note updated with the Advanced flow and its scientific-review list |

## Round 4 (branch `design/v4-quality-round4`)

Plan: [v4-round4-plan.md](v4-round4-plan.md) · three plan reviews: [visual](v4-round4-review-visual.md), [interaction](v4-round4-review-interaction.md), [learning](v4-round4-review-learning.md) · reconciled work list: [v4-round4-worklist.md](v4-round4-worklist.md)

| # | Item | Status | Notes |
|---|---|---|---|
| R4-1 | First-run cue and the body Enter defect | Done | Each trigger row now names the everyday event in the learner's own vocabulary ("an exam, a near miss, a fright") instead of describing the lesson, and `syn` carries those words so search finds the scene by what happened. While nothing is chosen the rows carry a warm left-edge accent, re-driven from `syncTriggers` so appearance and state cannot drift, and gone the moment a trigger is pressed. **Fixed the task-1 defect the plan reviews measured:** the first Tab stop on first paint is `#world`, and Enter there did nothing (`HS.E.state` stayed `idle`), so the first action a keyboard learner could take led nowhere; Enter (or Space) on the body now opens the first trigger. Checked by script: 16/18 comprehension (task 1 fully green, including the behavioural half), 95/95 quality bar. The audit also gained a Play-in-flight label probe, which is the one state that already reaches the label ceiling |
| R4-2 | The unrevealed line says so in its shape | Done | The unrevealed route now reads as an *unfinished* line rather than a dashed association: it carries no end glyph, where every other route kind ends in one (arrow / bar / diamond), and the `?` badge sits where it stops. An always-on "**Not revealed yet**" label sits on the badge's own dashed plate, reusing the system's existing "not real / not yet" grammar (`.draft`, `.card .ev`, `.thought`) so the claim survives grayscale at no reading cost. The "what to do about it" instruction goes through the one-shot, dismissible, Settings-switchable `HS.tip` channel instead of a second permanent label, and the label's span is `aria-hidden` so the live region does not announce it twice. Gated with no level test, because `dark:night` opens at organ level. Fixes a latent §10 breach found while building it: during ▶ Play the current step's own label and its route name both appeared ("Hypothalamus releases CRH" **and** "CRH · portal"), so the duplicate took `stress:slow` to 9 labels, over the ceiling of 8. The route name is now suppressed when the step label already names the same signal — repetition removed, not content. Checked: every pathway stays ≤ 8 labels at its playing peak, 95/95 quality bar, task 5 fully green |
| R4-3 | The not-a-vessel answer arrives where the learner is looking | Done | The "schematic: not a drawing of a blood vessel or a nerve" line now sits on every route card, one click from the body, immediately beside the texture swatch that is the visual evidence for the claim — the moment of confusion, not a panel the learner has to know exists. Inside Read the route it moves to the top of the sheet in the system's dashed "not real / not yet" treatment; `openRead` also gained the `scrollTop=0` that `openMore` and `openPassport` already had, so the sheet opens at the top instead of leaving the old disclaimer 417 px below the fold in a 636 px viewport. The reviews cut this item's original plan — prefixing *every* route name with "schematic" — because repeating one word on six route names in pathways already at the label ceiling is habituation, and it reversed the direction's own §5.3/§7 rule (schematic route on first appearance, then the short name). Task 6 fully green |
